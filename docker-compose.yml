services:
  redis:
    image: redis:latest
    container_name: auction-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "tagtag1010"]
    restart: unless-stopped

volumes:
  redis-data: